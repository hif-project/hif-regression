{
    "note": "An ordinary combinational for loop over a module-level integer. CURRENTLY FAILS, DELIBERATELY, on hif-backend#80: the loop index is shadowed and the write-back appended to the init and step clauses with a comma, which is not Verilog. Reachable from the plainest use of the construct - a population count - with no generate and no disable involved.",
    "expected_failure": {"issue": "hif-backend#80", "stage": "reparse"}
}
